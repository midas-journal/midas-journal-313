FIND_PACKAGE(ITK)
IF(ITK_FOUND)
    INCLUDE(${ITK_USE_FILE})
ENDIF(ITK_FOUND)

INCLUDE_DIRECTORIES( BEFORE ${ITK_INCLUDE_DIR} )

ADD_EXECUTABLE( smoothing smoothing.cxx )
TARGET_LINK_LIBRARIES( smoothing ITKCommon ITKQuadEdgeMesh )

INCLUDE(CTEST)

